// Stacks
// Stacks only have two operations
// .push() & .pop()

// Stack is a class with 4 methods
// .push - adds to top 0(1)
// .pop - removes from top 0(1)
// .peek - looks at top 0(1)
// .size() 0(1)
// Stacks have no random access

// is good with undo/redo operations
// parenthesis matching
// function calls