// Lists
// A data structure built out of an array
// Appears to have a 'squishy' size where elements can be added or removed
// Supports operations like:
let x = ll.get(index)
ll.set(index, value)
ll.removeFromIndex(index)
ll.insertAtIndex(index, value)